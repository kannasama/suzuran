# Secrets

This directory holds any secret or credential files used locally.

**Do not commit actual secret values.**

The `secrets/` directory is tracked to ensure it exists in the repository; secret files themselves are gitignored.
